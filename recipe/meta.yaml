{% set name = "xdem" %}
{% set version = "0.0.7" %}

package:
  name: {{ name | lower }}
  version: {{ version }}

source:
  url: https://github.com/GlacioHack/xdem/archive/refs/tags/v{{ version }}.tar.gz
  sha256: e85ecc68535b247e8cc88205cc02e4be0e0f2e055008a729cdaee23b1fa097c8

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.7
    - pip

  run:
    - python >=3.7
    - proj >=7.2
    - geopandas
    - numba
    - matplotlib-base
    - opencv
    - pyproj
    - rasterio
    - scipy
    - tqdm
    - scikit-image
    - scikit-gstat >=0.6.7
    - pytransform3d
    - geoutils

test:
  imports:
    - xdem

about:
  home: https://github.com/GlacioHack/xdem
  license: MIT
  license_file: LICENSE
  summary: Set of tools to manipulate Digital Elevation Models (DEMs)
  doc_url: https://xdem.readthedocs.io
  dev_url: https://github.com/GlacioHack/xdem

extra:
  recipe-maintainers:
    - erikmannerfelt
    - adehecq
    - rhugonnet
