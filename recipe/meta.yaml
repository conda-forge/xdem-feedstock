{% set name = "xdem" %}
{% set version = "0.0.12pre" %}

package:
  name: {{ name | lower }}
  version: {{ version }}

source:
  url: https://github.com/GlacioHack/xdem/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 9bfdae04a074d05d77df9a5891f6b3c41d3852c0e69bcf1be512ac7c6717fea3

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.8
    - pip

  run:
    - python >=3.8
    - proj >=7.2
    - geopandas >=0.10.0
    - fiona
    - shapely
    - numba
    - numpy
    - matplotlib
    - opencv
    - openh264
    - pyproj >=3.4
    - rasterio >=1.3
    - scipy
    - tqdm
    - scikit-image
    - scikit-gstat >=1.0
    - pytransform3d
    - geoutils ==0.0.11

test:
  imports:
    - xdem

about:
  home: https://github.com/GlacioHack/xdem
  license: MIT
  license_file: LICENSE
  summary: Analysis of digital elevation models (DEMs)
  doc_url: https://xdem.readthedocs.io
  dev_url: https://github.com/GlacioHack/xdem

extra:
  recipe-maintainers:
    - erikmannerfelt
    - adehecq
    - rhugonnet
